export namespace errorCode {
  export enum errCode {
    badData = -1,
    enrolExist = 100,
    noOpenId,
    cancelError,
    enrolUpdateError,
    enrolError,
    assignError,
    deleteGameError,
  }
}
