export namespace errorCode {
  export enum errCode {
    badData = -1,
    enrolExist = 100,
    createGameError,
    queryGameError,
    noOpenId,
    cancelError,
    enrolUpdateError,
    enrolError,
    assignError,
    deleteGameError,

    refereeRegistError,
    refereeShowError,
  }
}
